from django.conf import settings
from django.urls import include, path
from django.contrib import admin
from .forms import SizeForm
from . import views

urlpatterns = [
    # path('size/', views.add_size, name='size'),

]